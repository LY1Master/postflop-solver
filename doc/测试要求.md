# 测试要求
每次执行全量测试时应执行以下要求
1.记录的数据应包括每个牌面oop ip 每手牌的手牌，所属的桶，FullEV,无修正情况下DLEV,本次修正方案修正的DLEV(例如DLEVv2 DLEVv3),无修正DLEV与FullEV的偏差百分比，修正DLEV与Full的偏差百分比，修正结果是改善、恶化、没改变;
2.每个牌面的情况以.csv格式文件保存，文件名为牌面+手牌类型(三条面，公对面等)+spr；

